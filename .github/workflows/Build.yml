name: build-and-test

on: push

jobs:

  BuildAndTest:

    runs-on: ubuntu-latest
    steps:
    
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Installing valgring and cpplint
      run: |
          sudo apt install valgrind
          sudo pip install cpplint
          
    - name: Test them all
      run: make Lab4_1_test -C Lab4/Lab4_1
